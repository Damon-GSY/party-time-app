.vote-page {
  background: var(--color-bg);
}

.vote-content {
  padding: 34rpx 40rpx calc(190rpx + env(safe-area-inset-bottom));
}

.event-note {
  display: block;
  margin-top: 18rpx;
  padding-left: 18rpx;
  color: var(--color-text-secondary);
  border-left: 4rpx solid var(--color-border);
  font-size: 24rpx;
  line-height: 1.6;
}

.nickname-field {
  margin-top: 46rpx;
  animation-delay: 60ms;
}

.selection-help {
  display: block;
  margin-top: -12rpx;
  color: var(--color-text-muted);
  font-size: 23rpx;
}

.date-scroll {
  width: calc(100% + 40rpx);
  margin-top: 28rpx;
}

.date-tabs {
  display: inline-flex;
  gap: 12rpx;
  padding-right: 40rpx;
}

.date-tab {
  width: 112rpx;
  min-height: 102rpx;
  margin: 0;
  padding: 12rpx 8rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  background: var(--color-surface);
  color: var(--color-text-secondary);
  border: 2rpx solid var(--color-border);
  border-radius: var(--radius-md);
}

.date-tab.is-active {
  background: var(--color-coral-soft);
  color: var(--color-coral);
  border-color: var(--color-coral);
}

.date-weekday {
  font-size: 21rpx;
}

.date-day {
  margin-top: 4rpx;
  font-size: 26rpx;
  font-weight: 600;
}

.day-toolbar {
  margin-top: 34rpx;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 20rpx;
}

.day-title {
  display: block;
  font-size: 29rpx;
  font-weight: 600;
}

.day-caption {
  display: block;
  margin-top: 4rpx;
  color: var(--color-text-muted);
  font-size: 22rpx;
}

.day-actions {
  display: flex;
  gap: 10rpx;
}

.compact-button {
  min-width: 104rpx;
  min-height: 88rpx;
  margin: 0;
  padding: 0 20rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: transparent;
  color: var(--color-text-secondary);
  border: 2rpx solid var(--color-border);
  border-radius: var(--radius-pill);
  font-size: 23rpx;
}

.compact-button.is-accent {
  color: var(--color-coral);
  border-color: rgba(255, 118, 104, 0.5);
}

.compact-button[disabled] {
  opacity: 0.4;
}

.slots-grid {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 12rpx;
  margin-top: 24rpx;
  transition: opacity 100ms ease, transform 100ms ease;
}

.slots-grid.is-hidden {
  opacity: 0;
  transform: translateY(8rpx);
}

.slot-button {
  min-width: 0;
  min-height: 108rpx;
  margin: 0;
  padding: 12rpx 8rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  background: var(--color-surface);
  color: var(--color-text-secondary);
  border: 2rpx solid var(--color-border);
  border-radius: var(--radius-md);
  transition: transform var(--duration-fast) var(--ease-out), background-color var(--duration-fast) ease, border-color var(--duration-fast) ease;
}

.slot-button:active {
  transform: scale(0.95);
}

.slot-button.is-selected {
  background: var(--color-coral);
  color: #1a1412;
  border-color: var(--color-coral);
}

.slot-label {
  font-size: 23rpx;
  font-weight: 600;
}

.slot-state {
  margin-top: 6rpx;
  font-size: 19rpx;
  opacity: 0.76;
}

.vote-footer {
  position: fixed;
  right: 0;
  bottom: 0;
  left: 0;
  z-index: 20;
  min-height: 130rpx;
  padding: 20rpx 40rpx;
  box-sizing: border-box;
  display: flex;
  align-items: center;
  gap: 24rpx;
  background: var(--color-bg);
  border-top: 2rpx solid var(--color-border);
}

.selected-summary {
  min-width: 120rpx;
  display: flex;
  align-items: baseline;
  gap: 8rpx;
}

.selected-number {
  color: var(--color-coral);
  font-size: 42rpx;
  font-weight: 700;
}

.selected-label {
  color: var(--color-text-secondary);
  font-size: 22rpx;
}

.vote-submit {
  min-height: 92rpx;
  margin: 0;
  flex: 1;
}

.state-action {
  min-width: 240rpx;
  margin-top: 32rpx;
}

.success-modal {
  position: fixed;
  inset: 0;
  z-index: 50;
  visibility: hidden;
  pointer-events: none;
}

.success-modal.is-open {
  visibility: visible;
  pointer-events: auto;
}

.modal-backdrop {
  position: absolute;
  inset: 0;
  background: rgba(0, 0, 0, 0.66);
  opacity: 0;
  transition: opacity 220ms ease;
}

.success-modal.is-open .modal-backdrop {
  opacity: 1;
}

.success-sheet {
  position: absolute;
  right: 0;
  bottom: 0;
  left: 0;
  padding: 48rpx 40rpx calc(34rpx + env(safe-area-inset-bottom));
  background: var(--color-surface-raised);
  border-radius: 36rpx 36rpx 0 0;
  border-top: 2rpx solid var(--color-border);
  transform: translateY(100%);
  transition: transform 280ms var(--ease-out);
}

.success-modal.is-open .success-sheet {
  transform: translateY(0);
}

.success-icon {
  width: 96rpx;
  height: 96rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--color-coral-soft);
  border-radius: 50%;
}

.success-title {
  display: block;
  margin-top: 26rpx;
  font-size: 38rpx;
  font-weight: 700;
}

.success-copy {
  display: block;
  margin: 12rpx 0 30rpx;
  color: var(--color-text-secondary);
  font-size: 25rpx;
}

.modal-primary,
.modal-secondary {
  width: 100%;
  margin: 16rpx 0 0;
}

.reminder-button {
  min-height: 88rpx;
  margin: 18rpx 0 0;
  background: transparent;
  color: var(--color-coral);
  font-size: 24rpx;
}
